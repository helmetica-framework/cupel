apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-demo
spec:
  replicas: {{ .Values.replicas }}
  template:
    spec:
      containers:
        - name: demo
          image: {{ .Values.image }}
